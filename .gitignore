node_modules/
dist/
out/
*.log
.DS_Store
Thumbs.db
.env
.env.local

# 草稿区：简报、抓下来的数据、一次性脚本、临时输出。清了能重建的才配住这里
tmp/

# Rust 构建产物。实测这个目录有 5.6 GB，一旦提交仓库就没法看了
target/

# tauri-build 每次构建都会重新生成，不是源码
**/src-tauri/gen/
